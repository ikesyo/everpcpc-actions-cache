name: "save cloud cache"
author: everpcpc
description: "Save Actions cache to a cloud provider with OpenDAL."
inputs:
  provider:
    description: "Storage provider"
    required: true
    default: "s3"
  endpoint:
    description: "Storage provider endpoint"
    required: false
  bucket:
    description: "Storage backend bucket"
    required: true
  root:
    description: "Root directory for all cache files"
    required: false
    default: ""
  path:
    description: "A list of files, directories, and wildcard patterns to cache"
    required: true
  key:
    description: "An explicit key for saving the cache"
    required: true
  use-fallback:
    description: "Use github actions/cache as fallback"
    required: false
    default: "false"
runs:
  using: node24
  main: "../dist/save-only/index.js"
branding:
  icon: "archive"
  color: "gray-dark"
